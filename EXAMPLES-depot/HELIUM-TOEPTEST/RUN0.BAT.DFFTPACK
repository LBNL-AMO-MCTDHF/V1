#!/bin/bash

source "RUN.H"

export OMP_NUM_THREADS=$threads
mpirun  -n $procs -npernode $NPN --bind-to-core --bysocket --cpus-per-proc $threads --report-bindings chmctdhf_sinc.dfftpack.debug $1 Inp=Input.Inp.States0 $OPTION |tee Outs/Out.states0.dfftpack



